import styled from "styled-components";

const StyledInput = styled.input`
    width: 100%;
    font-size: 1.6rem;
    text-align: center;
    border: none;
    cursor: text;

    &:focus {
        outline: none;
    }
`;

export default StyledInput;
